#include <stdio.h>
#include <math.h>

/**
 *
 */
int main(void)
{
	int start = 1;
	int mynum;
	int add;
	int substract;
	int multiply;
	int divide;
	int quit = 0;

	printf("Welcome , would you like to start the calculator with 1 ou quit with 0 ?\n");
		scanf("%d", &mynum);

	if (mynum = 1)
	{
	}
	else
		return(0);
}
