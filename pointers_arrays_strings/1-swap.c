#include "main.h"

/**
 * swap_int - let you swap the two pointer int
 *
 * @a: character
 *
 * @b: character
 *
 * Return: Always 0 (Success)
 */
void swap_int(int *a, int *b)
{
	int temp;

	*a = temp;
	*a = *b;
	*b = temp;
}
