#include "main.h"

/**
 * print_sign - check the code
 *
 * @c: character
 *
 * Return: Always 0 (Success)
 */
int print_sign(int c)
{
	if (c > 0)
	{
		_putchar('+');
		if (c == 0)
			_putchar(0);
		else
			_putchar('-');
	else 
	{
	}
	}
	return (0);
}
	
